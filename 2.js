function isEven(value) {
    if (value % 2 == 0) {
        return true;
    }
    else
        return false;
    }
}

//O(1) because no matter how big N is, it will always take the same amount of time to solve.